@{
  Layout = "_Layout";
}

@model StudentRandomizerMvc.Models.Student

<h2>Edit</h2>

<h4>Edit: @Html.DisplayFor(model => model.Name)</h4>


<h1>Edit Info:</h1>
@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.StudentId)
  @Html.Label("Name")
  @Html.TextBoxFor(model => model.Name)
  <button type="submit" class="btn btn-primary" title="Save Student">Save Student</button>
}

<p> @Html.ActionLink("Back to Student List", "Index") </p>